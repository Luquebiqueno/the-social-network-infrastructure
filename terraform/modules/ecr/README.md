# ECR Module

Creates encrypted Amazon ECR repositories for application Docker images.

Repositories enable image scanning, use immutable tags by default, remove untagged images after seven days, and retain a configurable number of recent tagged images.
